from enum import Enum

class DisplayMode(Enum):
    """
    DisplayMode Enum

    This enum represents different display modes of a projector.

    Enum Values:
        SPORT: Sport mode.
        ACTIVE: Active mode.
        PRESENTATION: Presentation mode.
    """
    SPORT = 1
    ACTIVE = 2
    PRESENTATION = 3
